const API = '/api';
const STOCK_MARKET = API + '/company';

export const ESTOCK_MARKET = {
    REGISTER_COMPANY: `${API}/register`,
    COMPANY_INFO: `${API}/info`,
    ALL_COMPANY: `${API}/allCompanies`
  }